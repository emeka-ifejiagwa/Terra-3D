import SwiftUI
import simd
import Accelerate

struct ContentView: View {
    var body: some View {
        VStack {
            Button {
                
            } label: {
                Text("Generate Noise")
            }
        }
    }
}
